import React from 'react'

const JSEnvironmentTasks: React.FC = () => {
	return (
		<>
			Environment Tasks
			{/*{codeTasks.map((task, id) => {*/}
			{/*	return <Task key={id} task={task} id={id} />*/}
			{/*})}*/}
		</>
	)
}

export default JSEnvironmentTasks
